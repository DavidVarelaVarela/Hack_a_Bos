// El objeto Window
const windowHeight = window.innerHeight;
const windowWidth = Window.innerwWidth;
console.log("Altura y anchura del viewport:");
console.log(windowHeight + "px", windowWidth + "px");

const completeWindow = window.outerHeight;
const completeWindow = window.outerWidth;

console.log("Altura y anchura de la ventana del navegador:");
console.log(completewindowHeight + "px", completewindowWidth + "px");
